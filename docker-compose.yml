services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: approval_platform_poc
      POSTGRES_USER: poc
      POSTGRES_PASSWORD: poc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

